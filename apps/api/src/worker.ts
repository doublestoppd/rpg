/**
 * Job worker process entrypoint.
 *
 * The pg-boss worker is implemented in Phase 1. Phase 0 only reserves the
 * process command contract: `npm run start:worker` starts this process.
 * In production the worker always runs as a separate process from the API.
 */

console.error('The job worker is not implemented yet (Phase 1). This is the Phase 0 placeholder.');
process.exit(1);
