/**
 * @rpg/shared — API contract package.
 *
 * Shared Zod request/response schemas, enums, and public types live here from
 * Phase 1 onward. Internal database types must never be exported from this
 * package; it defines only the public API contract between apps/api and
 * apps/web.
 */

export const SHARED_PACKAGE_NAME = '@rpg/shared';
